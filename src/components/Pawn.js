import Figure from './Figure';
import React, { PropTypes } from 'react';

export default class Pawn extends Figure {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                1
            </div>
        );
    }
}

